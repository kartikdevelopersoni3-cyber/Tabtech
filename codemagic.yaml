workflows:
android-debug:
name: Tabtech Android Debug Build

environment:
  java: 17

scripts:
  - chmod +x gradlew
  - ./gradlew clean
  - ./gradlew assembleDebug

artifacts:
  - app/build/outputs/**/*.apk

publishing:
  email:
    recipients:
      - ritusoniji1983@gmail.com
